<ContentDialog
    x:Class="Task_Manager_UWP.Views.AddDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Task_Manager_UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:UWPMVVMLib.Converters"
    mc:Ignorable="d"
    Title="Add a task"
    IsPrimaryButtonEnabled="True"
    IsSecondaryButtonEnabled="False"
    PrimaryButtonText="Add"
    CloseButtonText="Cancel">
    <ContentDialog.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </ContentDialog.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding TaskName, Mode=TwoWay}" PlaceholderText="Name"/>
        <TextBox Grid.Row="1" Text="{Binding Description, Mode=TwoWay}" PlaceholderText="Description"/>
        <ComboBox Grid.Row="2" ItemsSource="{Binding TaskTypes}" SelectionChanged="Selector_OnSelectionChanged"/>
        <Grid Grid.Row="3" Visibility="{Binding IsProgress, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="7*"/>
                <RowDefinition Height="9*"/>
            </Grid.RowDefinitions>
            <TextBox Text="{Binding PointName, Mode=TwoWay}" PlaceholderText="Point of progress name" Grid.Row="0"/>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding DonePoints, Mode=TwoWay}" PlaceholderText="Done points"/>
                <TextBox Grid.Column="1" Text="{Binding AllPoints, Mode=TwoWay}" PlaceholderText="Points count"/>
            </Grid>
        </Grid>
    </Grid>
</ContentDialog>
